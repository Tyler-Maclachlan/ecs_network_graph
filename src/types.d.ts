type Options = {
  [key: string]: any;
};

type Entity = string;

type Shape =
  | 'circle'
  | 'triangle'
  | 'square'
  | 'rectangle'
  | 'image'
  | 'imageCircular';

type Vector2D = {
  x: number;
  y: number;
};
